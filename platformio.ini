; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]
extra_configs = secret.ini
default_envs = Test-Device

[extra]
logging =
		; -D IOT_NODE_LOGGING
build_flags_pre = 
	!echo "-D IOT_NODE_ENV_BUILD_GIT_REV="$(git rev-parse HEAD)
	-D IOT_NODE_ENV_PIO_ENV=$PIOENV
	-D IOT_NODE_ENV_PIO_PLATFORM=$PIOPLATFORM
	-D IOT_NODE_ENV_PIO_FRAMEWORK=$PIOFRAMEWORK
	-D IOT_NODE_ENV_OTA_PASSWORD="${secret.ota_password}"
	-include "include/utils/pre.h"
	-include "include/secret.h"
build_flags_post = 
	-include "include/utils/post.h"
build_flags_esp32 = 
	-include "include/envs/esp32.h"
build_flags_esp8266 = 
	-include "include/envs/esp8266.h"
lib_deps = 
	adafruit/Adafruit BME280 Library@^2.1.2
	adafruit/Adafruit BusIO@^1.7.2
	adafruit/Adafruit CCS811 Library@^1.0.5
	adafruit/Adafruit MCP9808 Library@^1.1.2
	adafruit/Adafruit SGP30 Sensor@^2.0.0
	adafruit/Adafruit TSL2561@^1.1.0
	adafruit/Adafruit Unified Sensor@^1.1.4
	adafruit/Adafruit VEML6070 Library@^1.0.4
	https://github.com/septillion-git/FadeLed.git#ESP32-support
	lewapek/Nova Fitness Sds dust sensors library@^1.5.0
	sui77/rc-switch@^2.6.4
	wifwaf/MH-Z19@^1.5.3
lib_deps_esp32 = 
	https://github.com/Xinyuan-LilyGO/LilyGo-EPD47.git
lib_deps_esp8266 = 
	me-no-dev/ESPAsyncUDP
serial_bridge = socket://serial.lan.wurstsalat.cloud:23

[env]
framework = arduino
upload_protocol = espota
; upload_port =
; upload_port = ${extra.serial_bridge}
monitor_port = ${extra.serial_bridge}
board_build.filesystem = littlefs
monitor_speed = 115200
upload_speed = 115200

[env_esp32]
build_flags_pre = 
	${extra.build_flags_pre}
	${extra.build_flags_esp32}
	${extra.logging}
platform = espressif32
build_flags = 
	${build_flags_pre}
	${extra.build_flags_post}
lib_deps = 
	${extra.lib_deps}
	${extra.lib_deps_esp32}
upload_flags =
  --port=3232
	--auth=${secret.ota_password}

[env_esp8266]
build_flags_pre = 
	${extra.build_flags_pre}
	${extra.build_flags_esp8266}
	${extra.logging}
platform = espressif8266
build_flags = 
	${build_flags_pre}
	${extra.build_flags_post}
lib_deps = 
	${extra.lib_deps}
	${extra.lib_deps_esp8266}
upload_flags = 
	--auth=${secret.ota_password}

; TESTBED DEVICES

[env:Test-Device]
extends = env_esp32
upload_port = test-device.iot-ng.lan.wurstsalat.cloud
name = Test-Device
board = esp32dev
board_build.flash_mode = dio
build_flags = 
	-D IOT_NODE_ENV_NAME="${name}"
	${env_esp32.build_flags_pre}
	-D IOT_NODE_LINK_ETH
	-include "include/boards/Test-Device.h"
	${extra.build_flags_post}

[env:Shelly1]
extends = env_esp8266
upload_port = shelly1.iot-ng.lan.wurstsalat.cloud
name = Shelly1
board = esp01_1m
board_build.flash_mode = qio
build_flags = 
	-D IOT_NODE_ENV_NAME="${name}"
	${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly1.h"
	${extra.build_flags_post}

[env:Obi-Jack]
extends = env_esp8266
upload_port = obi-jack.iot-ng.lan.wurstsalat.cloud
name = Obi-Jack
board = esp01_1m
board_build.flash_mode = qio
build_flags = 
	-D IOT_NODE_ENV_NAME="${name}"
	${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Obi-Jack.h"
	${extra.build_flags_post}

[env:H801]
	extends = env_esp8266
	upload_port = h801.iot-ng.lan.wurstsalat.cloud
	name = H801
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/H801.h"
		${extra.build_flags_post}

[env:Shelly i3]
	extends = env_esp8266
	upload_port = shelly-i3.iot-ng.lan.wurstsalat.cloud
	name = Shelly_i3
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}
	
[env:Olimex ESP32-PoE]
	extends = env_esp32
	upload_port = olimex-esp32-poe.iot-ng.lan.wurstsalat.cloud
	name = Olimex_ESP32-PoE
	board = esp32dev
	board_build.flash_mode = dio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp32.build_flags_pre}
		-D IOT_NODE_LINK_ETH
		-include "include/boards/Olimex_ESP32-PoE.h"
		${extra.build_flags_post}

[env:ESP-Now Test-Button]
	extends = env_esp8266
	upload_port = esp-now-test-button.iot-ng.lan.wurstsalat.cloud
	name = ESP-Now_Test-Button
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/ESP-Now_Test-Button.h"
		${extra.build_flags_post}

[env:Olimex ESP32-Gateway]
	extends = env_esp32
	upload_port = olimex-esp32-gateway.iot-ng.lan.wurstsalat.cloud
	name = Olimex_ESP32-Gateway
	board = esp32dev
	board_build.flash_mode = dio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp32.build_flags_pre}
		-D IOT_NODE_LINK_ETH
		-include "include/boards/Olimex_ESP32-Gateway.h"
		${extra.build_flags_post}

[env:ESP-Now Test-Window-Sensor]
	extends = env_esp8266
	upload_port = esp-now-test-window-sensor.iot-ng.lan.wurstsalat.cloud
	name = ESP-Now_Test-Window-Sensor
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/ESP-Now_Test-Window-Sensor.h"
		${extra.build_flags_post}

[env:ESP-Now Test-Button-II]
	extends = env_esp8266
	upload_port = 0.0.0.0
	name = ESP-Now_Test-Button-II
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/ESP-Now_Test-Button-II.h"
		${extra.build_flags_post}

[env:LilyGo 4.7 inch e-paper]
	extends = env_esp32
	upload_port = lilygo-e-paper.iot-ng.lan.wurstsalat.cloud
	name = LilyGo-E-Paper
	board = esp32dev
	board_build.flash_mode = dio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp32.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-D BOARD_HAS_PSRAM
		-include "include/boards/LilyGo-E-Paper.h"
		${extra.build_flags_post}

[env:wireless-tag WT32-SC01]
	extends = env_esp32
	upload_port = wt32-sc01.iot-ng.lan.wurstsalat.cloud
	name = WT32-SC01
	board = esp32dev
	board_build.flash_mode = dio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp32.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-D BOARD_HAS_PSRAM
		-include "include/boards/WT32-SC01.h"
		${extra.build_flags_post}

[env:Sonoff Basic R2-v1.3]
	extends = env_esp8266
	upload_port = sonoff-basic.iot-ng.lan.wurstsalat.cloud
	name = Sonoff_Basic
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Sonoff_Basic_R2V13.h"
		${extra.build_flags_post}

[env:Shelly Plus 1]
	extends = env_esp32
	upload_port = shelly-plus-1.iot-ng.lan.wurstsalat.cloud
	name = Shelly_Plus_1
	board = esp32doit-devkit-v1
	board_build.flash_mode = dio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp32.build_flags_pre}
		-D IOT_NODE_LINK_ETH
		-D CONFIG_FREERTOS_UNICORE=YES
		-include "include/boards/Shelly_Plus_1.h"
		${extra.build_flags_post}

	; REAL WORLD DEVICES

[env:Shelly1, storage, ceilingLight]
	extends = env_esp8266
	upload_port = storage-ceilinglight.iot.wurstsalat.cloud
	name = storage-ceilingLight
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly1.h"
		${extra.build_flags_post}

[env:Sonoff Basic R2-v1.3, office, ceilingLight]
	extends = env_esp8266
	upload_port = office-ceilinglight.iot.wurstsalat.cloud
	name = office-ceilingLight
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-D IOT_NODE_POWER_CYCLE_PROTECTION
		-include "include/boards/Sonoff_Basic_R2V13.h"
		${extra.build_flags_post}

[env:H801, office, workbenchLEDs]
	extends = env_esp8266
	upload_port = office-workbenchleds.iot.wurstsalat.cloud
	name = office-workbenchLEDs
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/H801.h"
		${extra.build_flags_post}

[env:Obi-Jack, office, floodlight]
	extends = env_esp8266
	upload_port = office-floodlight.iot.wurstsalat.cloud
	name = office-floodlight
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Obi-Jack.h"
		${extra.build_flags_post}

[env:Shelly i3, office, wallSwitch]
	extends = env_esp8266
	upload_port = office-wallswitch.iot.wurstsalat.cloud
	name = office-wallSwitch
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Obi-Jack, livingRoom, standingLamp]
	extends = env_esp8266
	upload_port = livingroom-standinglamp.iot.wurstsalat.cloud
	name = livingRoom-standingLamp
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Obi-Jack.h"
		${extra.build_flags_post}

[env:Obi-Jack, diningRoom, standingLamp]
	extends = env_esp8266
	upload_port = diningroom-standinglamp.iot.wurstsalat.cloud
	name = diningRoom-standingLamp
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Obi-Jack.h"
		${extra.build_flags_post}

[env:Shelly1, bedroom, ceilingLight]
	extends = env_esp8266
	upload_port = bedroom-ceilinglight.iot.wurstsalat.cloud
	name = bedroom-ceilingLight
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly1.h"
		${extra.build_flags_post}

[env:Obi-Jack, bedroom, stoneLamp]
	extends = env_esp8266
	upload_port = bedroom-stonelamp.iot.wurstsalat.cloud
	name = bedroom-stoneLamp
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Obi-Jack.h"
		${extra.build_flags_post}

[env:H801, bedroom, bedRGBWLEDs]
	extends = env_esp8266
	upload_port = bedroom-bedrgbwleds.iot.wurstsalat.cloud
	name = bedroom-bedRGBWLEDs
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/H801.h"
		${extra.build_flags_post}

[env:H801, bedroom, nightstandLEDs]
	extends = env_esp8266
	upload_port = bedroom-nightstandleds.iot.wurstsalat.cloud
	name = bedroom-nightstandLEDs
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/H801.h"
		${extra.build_flags_post}

[env:Shelly i3, bedroom, wallSwitchDoor]
	extends = env_esp8266
	upload_port = bedroom-wallswitchdoor.iot.wurstsalat.cloud
	name = bedroom-wallSwitchDoor
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Obi-Jack, livingRoom, fan]
	extends = env_esp8266
	upload_port = livingroom-fan.iot.wurstsalat.cloud
	name = livingRoom-fan
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Obi-Jack.h"
		${extra.build_flags_post}

[env:Shelly i3, livingRoom, wallSwitch]
	extends = env_esp8266
	upload_port = livingroom-wallswitch.iot.wurstsalat.cloud
	name = livingRoom-wallSwitch
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Sonoff Basic R2-v1.3, livingRoom, ceilingLight]
	extends = env_esp8266
	upload_port = livingroom-ceilinglight.iot.wurstsalat.cloud
	name = livingRoom-ceilingLight
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Sonoff_Basic_R2V13.h"
		${extra.build_flags_post}

[env:Sonoff Basic R2-v1.3, diningRoom, ceilingLight]
	extends = env_esp8266
	upload_port = diningroom-ceilinglight.iot.wurstsalat.cloud
	name = diningRoom-ceilingLight
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Sonoff_Basic_R2V13.h"
		${extra.build_flags_post}

[env:Sonoff Basic R2-v1, diningRoom, tableLight]
	extends = env_esp8266
	upload_port = diningroom-tablelight.iot.wurstsalat.cloud
	name = diningRoom-tableLight
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Sonoff_Basic_R2V1.h"
		${extra.build_flags_post}

[env:H801, diningRoom, kallaxLEDs]
	extends = env_esp8266
	upload_port = diningroom-kallaxleds.iot.wurstsalat.cloud
	name = diningRoom-kallaxLEDs
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/H801.h"
		${extra.build_flags_post}

[env:Shelly i3, diningRoom, wallSwitch]
	extends = env_esp8266
	upload_port = diningroom-wallswitch.iot.wurstsalat.cloud
	name = diningRoom-wallSwitch
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:H801, kitchen, LEDsRight]
	extends = env_esp8266
	upload_port = kitchen-ledsright.iot.wurstsalat.cloud
	name = kitchen-LEDsRight
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/H801.h"
		${extra.build_flags_post}

[env:H801, kitchen, LEDsLeft]
	extends = env_esp8266
	upload_port = kitchen-ledsleft.iot.wurstsalat.cloud
	name = kitchen-LEDsLeft
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/H801.h"
		${extra.build_flags_post}

[env:Obi-Jack, diningRoom, fan]
	extends = env_esp8266
	upload_port = diningroom-fan.iot.wurstsalat.cloud
	name = diningRoom-fan
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Obi-Jack.h"
		${extra.build_flags_post}

[env:Shelly1, hallway, ceilingLightFront]
	extends = env_esp8266
	upload_port = hallway-ceilinglightfront.iot.wurstsalat.cloud
	name = hallway-ceilingLightFront
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-D IOT_NODE_POWER_CYCLE_PROTECTION
		-include "include/boards/Shelly1.h"
		${extra.build_flags_post}

[env:Sonoff Basic R2-v1.3, hallway, ceilingLightBack]
	extends = env_esp8266
	upload_port = hallway-ceilinglightback.iot.wurstsalat.cloud
	name = hallway-ceilingLightBack
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Sonoff_Basic_R2V13.h"
		${extra.build_flags_post}

[env:Shelly1, showerBathroom, ceilingLight]
	extends = env_esp8266
	upload_port = showerbathroom-ceilinglight.iot.wurstsalat.cloud
	name = showerBathroom-ceilingLight
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-D IOT_NODE_POWER_CYCLE_PROTECTION
		-include "include/boards/Shelly1.h"
		${extra.build_flags_post}

[env:Shelly1, bathtubBathroom, ceilingLight]
	extends = env_esp8266
	upload_port = bathtubbathroom-ceilinglight.iot.wurstsalat.cloud
	name = bathtubBathroom-ceilingLight
	board = esp01_1m
	board_build.flash_mode = qio
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly1.h"
		${extra.build_flags_post}

[env:Shelly i3, kitchen, wallSwitchFront]
	extends = env_esp8266
	upload_port = kitchen-wallswitchfront.iot.wurstsalat.cloud
	name = kitchen-wallSwitchFront
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Shelly i3, kitchen, wallSwitchBack]
	extends = env_esp8266
	upload_port = kitchen-wallswitchback.iot.wurstsalat.cloud
	name = kitchen-wallSwitchBack
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Shelly i3, bathtubBathroom, wallSwitchMirror]
	extends = env_esp8266
	upload_port = bathtubbathroom-wallswitchmirror.iot.wurstsalat.cloud
	name = bathtubBathroom-wallSwitchMirror
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Shelly i3, showerBathroom, wallSwitchDoor]
	extends = env_esp8266
	upload_port = showerbathroom-wallswitchdoor.iot.wurstsalat.cloud
	name = showerBathroom-wallSwitchDoor
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Shelly i3, hallway, wallSwitchFront]
	extends = env_esp8266
	upload_port = hallway-wallswitchfront.iot.wurstsalat.cloud
	name = hallway-wallSwitchFront
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Shelly i3, hallway, wallSwitchMiddle]
	extends = env_esp8266
	upload_port = hallway-wallswitchmiddle.iot.wurstsalat.cloud
	name = hallway-wallSwitchMiddle
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Shelly i3, hallway, wallSwitchBack]
	extends = env_esp8266
	upload_port = hallway-wallswitchback.iot.wurstsalat.cloud
	name = hallway-wallSwitchBack
	board = esp01_1m
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Shelly_i3.h"
		${extra.build_flags_post}

[env:Sonoff Basic R2-v1, showerBathroom, mirrorLight]
	extends = env_esp8266
	upload_port = showerbathroom-mirrorlight.iot.wurstsalat.cloud
	name = showerBathroom-mirrorLight
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Sonoff_Basic_R2V1.h"
		${extra.build_flags_post}

[env:Sonoff Basic R2-v1, showerBathroom, nightLight]
	extends = env_esp8266
	upload_port = showerbathroom-nightlight.iot.wurstsalat.cloud
	name = showerBathroom-nightLight
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Sonoff_Basic_R2V1.h"
		${extra.build_flags_post}

[env:Sonoff Basic R2-v1, bathtubBathroom, nightLight]
	extends = env_esp8266
	upload_port = bathtubbathroom-nightlight.iot.wurstsalat.cloud
	name = bathtubBathroom-nightLight
	board = esp8285
	board_build.flash_mode = dout
	build_flags = 
		-D IOT_NODE_ENV_NAME="${name}"
		${env_esp8266.build_flags_pre}
		-D IOT_NODE_LINK_WIFI
		-include "include/boards/Sonoff_Basic_R2V1.h"
		${extra.build_flags_post}
